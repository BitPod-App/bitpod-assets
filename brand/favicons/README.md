# Favicons

Canonical favicon packages for BitPod surfaces.

Use this folder for source favicon sets and normalized browser/runtime exports.
Do not use this folder as a public CDN or Cloudflare worker input by default.
Delivery copies can be generated from these assets when a consuming app needs
same-origin favicon files.
